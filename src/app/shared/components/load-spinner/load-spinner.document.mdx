import { Meta, Canvas, Story, Markdown } from "@storybook/addon-docs";
import * as LoadSpinnerComponentStories from "./load-spinner.stories";

<Meta of={LoadSpinnerComponentStories} />

# Load Spinner

O componente `LoadSpinnerComponent` é responsável por renderizar um spin rotativo, a fim de melhorar a experiência do usuário.

## Exemplos de uso

<Canvas of={LoadSpinnerComponentStories.Default} />
