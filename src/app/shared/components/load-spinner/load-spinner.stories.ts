import { Meta, StoryObj } from '@storybook/angular';
import { LoadSpinnerComponent } from './load-spinner.component';

const meta: Meta<LoadSpinnerComponent> = {
  title: 'components/LoadSpinner',
  component: LoadSpinnerComponent,
};
export default meta;

type Story = StoryObj<LoadSpinnerComponent>;

export const Default: Story = {};
